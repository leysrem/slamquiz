{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Question'|trans }}{% endblock %}

{% block body %}
    <h1>Edit Question</h1>

    {{ include('question/_form.html.twig', {'button_label': 'Update'|trans}) }}

    <a class="btn btn-primary" href="{{ path('question_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
